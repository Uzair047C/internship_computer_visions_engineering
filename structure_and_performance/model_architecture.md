Collect Images
        ↓
Annotate Images (LabelImg, CVAT, Roboflow, etc.)
        ↓
YOLO Dataset
(images/ + labels/ + data.yaml)
        ↓
Train YOLO
(model.train())
        ↓
Validate
        ↓
Test
        ↓
Inference